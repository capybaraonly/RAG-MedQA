// Stub: document-related hooks are not needed in the simplified RAG-MedQA.
// These remain as no-ops for components that still import them.

export const useGetChunkHighlights = () => ({ highlights: [] });
export const useGetDocumentUrl = () => ({ url: '' });
export const useFetchDocumentThumbnailsByIds = () => ({ data: [] as string[] });
